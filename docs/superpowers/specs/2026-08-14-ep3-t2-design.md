# EP3-T2 — Desain Sinkronisasi Dokumentasi (Design Doc)

**Tanggal:** 2026-08-14
**Status:** Disetujui user (via keputusan brainstorming, 3 jawaban "Recommended").

## 1. Masalah

Dokumen `docs/ENGINE_ARCHITECTURE.md` dan `docs/DESIGN_SUMMARY.md` tidak merefleksikan implementasi nyata (baseline roadmap §1.1 usang: 93 test/84% vs kondisi nyata 192 test/99.9%, fitur EP1/EP2 sudah di-commit). Task EP3-T2 mewajibkan:
- §12.3: perjelas aksi Tianyuan Ling = panel modal penampil data via `GET /api/tianyuan` + `view()`, bukan aksi mutasi sesi.
- §16/§17: tandai fitur Fase 1 (Toko Web, Dinamisasi Resep, Cooldown Side Quest, Timer Respawn Monster, Jadwal Harian NPC, Layar Penutup Arc 1) sebagai `SELESAI (Verified)`.
- DESIGN_SUMMARY: catat penyelesaian AC Fase 1.

## 2. Temuan Verifikasi Kode (fakta, bukan asumsi)

Semua 6 fitur klaim Verified **sudah ada di kode**:
| Fitur | Bukti |
|---|---|
| Toko Web | `web/app.py` `merchant_shop` context + `web/static/app.js` modal shop (renderShop/actShop) |
| Dinamisasi Resep | `web/app.py` `recipes` context + app.js filter resep dinamis |
| Cooldown Side Quest | `src/engine/quest.py:237-265` `side_quest_cooldowns` + `state.py:76` |
| Timer Respawn Monster | `session.py:331-336` `last_hunt_time` vs `world.monster_respawn_hours` |
| Jadwal Harian NPC | `session.py:148` `_is_npc_available` + gate `_talk`/`_spar` |
| Layar Penutup Arc 1 | `session.py:491-503` `arc_summary` + cli banner + app.js `modal-arc-summary` |

## 3. Inkonsistensi Dokumen vs Kode (yang diperbaiki)

| # | Lokasi | Kondisi dokumen | Kondisi kode nyata |
|---|---|---|---|
| 1 | §12.2 | `/api/game/{id}/...`, `/api/health`, respons `{state, log_delta}` | `/api/state`, `/api/saves`, `/api/tianyuan`, `/api/new`, `/api/load`, `/api/action`, `/api/save`; respons `{ok, view, context}` (web/app.py:9-15, `_payload`) |
| 2 | §12.3 | aksi `open_tianyuan`/`close_tianyuan` sebagai mutasi sesi; tidak ada `hunt/search/save/choose` | handler map session.py:112-128; tianyuan = GET modal |
| 3 | §12.4 | `ui.mode`, `quest`, `tianyuan`, `flags`, `time` | `mode`, `current_quest`, `side_quests`, `memories`, `choose`, `arc_summary` (session.py:485-563) |
| 4 | §12.5 | "panel Tianyuan Ling toggle" | modal fetch `/api/tianyuan`; context = npcs/merchant_shop/recipes/npc_names/techniques/academy/item_names |
| 5 | §5.1/§17 | cooldown "belum diterapkan engine (§17)" | quest.py sudah terapkan |
| 6 | §4/§15 | daftar test tanpa `test_effects.py`, `test_saveload.py`, `test_conftest.py` | 3 file ada |
| 7 | DESIGN_SUMMARY | dokumen pra-pembangunan "untuk Review" | Fase 1 selesai & tervalidasi |

## 4. Keputusan User (dari brainstorming)

1. **§12.4**: update penuh sesuai `view()` aktual (bukan rujuk-kode).
2. **§17**: pertahankan observasi playtest sebagai catatan Fase 2 (Han Xiu undertuned dsb.), hanya update status fitur → Verified.
3. **Changelog**: tambah riwayat pembaruan ringkas di header kedua dokumen.
4. Evaluasi rencana menyetujui: penandaan Verified dipusatkan di §17 (+ catatan cooldown §5.1); cek lintas-doc `rg` di akhir sebagai bukti AC.

## 5. Lingkup

Hanya 2 file: `docs/ENGINE_ARCHITECTURE.md`, `docs/DESIGN_SUMMARY.md`. Tidak menyentuh `data/`, `src/`, `web/`, `tests/` → validator & pytest tidak terdampak. Validasi = `rg` manual + inspeksi teks.

## 6. AC EP3-T2 (dipetakan)

- [x] ENGINE_ARCHITECTURE selaras 100% dengan kontrak endpoint `web/app.py` & struktur `session.py` → §12.2, §12.3, §12.4 ditulis ulang dari kode.
- [x] Tidak ada referensi bertentangan antar dokumen di `docs/` → cek `rg` lintas-doc di akhir (endpoint usang, `ui.mode`, `open_tianyuan`, `/api/game`, `/api/health`).
