# EP3-T2 — Sinkronisasi Dokumen Arsitektur & Desain ke Kode Nyata

> **For agentic workers:** implementasi inline (dokumentasi murni, tanpa kode/test baru). Setiap task = edit teks + verifikasi `rg`.

**Goal:** Membuat `docs/ENGINE_ARCHITECTURE.md` selaras 100% dengan kontrak `web/app.py` + struktur `session.py`, dan `docs/DESIGN_SUMMARY.md` mencatat penyelesaian AC Fase 1 — memenuhi kriteria penerimaan EP3-T2 tanpa mengubah kode/data.

**Architecture:** Semua fakta bersumber dari verifikasi kode nyata (bukan roadmap usang): endpoint `web/app.py:9-15` + `_payload()` (`{ok, view, context}`), handler map `session.py:112-128`, `view()` `session.py:485-563`, `_context()` `web/app.py:40-103`, `_tianyuan_payload()` `web/app.py:110-135`. Fitur Verified sudah dikonfirmasi ada (design doc §2).

**Tech Stack:** Markdown. Verifikasi: `rg` (bukan pytest — tidak ada perubahan kode).

## Global Constraints
- Hanya 2 file disentuh: `docs/ENGINE_ARCHITECTURE.md`, `docs/DESIGN_SUMMARY.md`. Dilarang mengubah `data/`, `src/`, `web/`, `tests/`.
- Bahasa Indonesia, istilah pinyin sesuai AGENTS.md.
- Changelog ringkas ditambahkan di header kedua dokumen (keputusan user).
- Bukti AC: `rg` di akhir memastikan tidak ada sisa referensi usang (`/api/game`, `/api/health`, `ui.mode`, `open_tianyuan`/`close_tianyuan` di ENGINE_ARCHITECTURE) dan tidak ada kontradiksi lintas-docs.

---

### Task 1: ENGINE_ARCHITECTURE — Header riwayat + §4 daftar test

**Files:** `docs/ENGINE_ARCHITECTURE.md` (baris 1-7, 99-108)

- [ ] **Step 1:** Tambah baris Riwayat di header (setelah baris "> **Fase 1**: ..."):
```markdown
> **Riwayat**: 2026-08-14 — sinkronisasi EP3-T2: endpoint API, kontrak state, status fitur Verified (toko web, resep dinamis, cooldown side quest, respawn monster, jadwal NPC, layar penutup Arc 1).
```
- [ ] **Step 2:** Perbarui daftar test §4 (tambah `conftest.py`, `test_effects.py`, `test_saveload.py`):
```
├── tests/                      # pytest
│   ├── conftest.py             # helper: finish_dialog, move_path, play_to_incident, god_mode
│   ├── test_quest_dag.py       # invariant DAG, satu-aktif, konvergensi
│   ├── test_dialog.py
│   ├── test_battle.py
│   ├── test_cultivation.py
│   ├── test_validator.py
│   ├── test_session.py
│   ├── test_effects.py         # dispatcher efek quest/dialog
│   ├── test_saveload.py        # round-trip save/load, save rusak
│   ├── test_companion.py
│   ├── test_cli.py
│   └── test_web.py
```
- [ ] **Step 3:** Verifikasi: `ls tests/` → nama 12 file cocok daftar baru; `rg "test_effects|test_saveload|conftest" docs/ENGINE_ARCHITECTURE.md` → 3 match.

---

### Task 2: ENGINE_ARCHITECTURE — §5.1 & §17 cooldown → SELESAI (Verified)

**Files:** `docs/ENGINE_ARCHITECTURE.md` (baris 186, 821-832)

- [ ] **Step 1:** §5.1 baris 186 field `cooldown`: ganti keterangan "belum diterapkan engine (§17)" → "divalidasi §14-8 (harus > 0 jika ada); diterapkan engine §6.4".
- [ ] **Step 2:** §17 baris "**Cooldown side quest**": ganti klaim "belum diterapkan engine — quest repeatable langsung tersedia lagi. Terapkan saat quest pertama memakainya (perlu mencatat waktu selesai di state)" → catatan selesai (Verified): diterapkan via `state.side_quest_cooldowns` (qid → absolute hour) & `quest.py` (`_can_offer`/`_offer`), termasuk timestamp waktu selesai.
- [ ] **Step 3:** Verifikasi: `rg -n "belum diterapkan" docs/ENGINE_ARCHITECTURE.md` → tanpa match; `rg -n "SELESAI" docs/ENGINE_ARCHITECTURE.md` → match di §17.

---

### Task 3: ENGINE_ARCHITECTURE — §12.2 endpoint aktual

**Files:** `docs/ENGINE_ARCHITECTURE.md` (baris 672-682)

- [ ] **Step 1:** Ganti seluruh tabel §12.2 dengan endpoint nyata `web/app.py:9-15`:
```
| Method & Path | Body | Balasan |
|---|---|---|
| `GET /api/state` | – | View sesi aktif (kontrak §12.4) atau `null` |
| `GET /api/saves` | – | Daftar slot save (menu utama "Lanjut") |
| `GET /api/tianyuan` | – | Payload panel Tianyuan Ling (mission/memories/system_log) |
| `POST /api/new` | – | `{"ok": true, "view": ..., "context": ...}` |
| `POST /api/load` | `{"name": "..."}` | `{"ok": true, "view": ..., "context": ...}` / 404 jika tak ada |
| `POST /api/action` | `{"action": ActionJSON (§12.3)}` | `{"ok": true, "view": ..., "context": ...}` |
| `POST /api/save` | `{"name": "..."}` | `{"ok": true, "view": ..., "context": ...}` (titik aman saja) |
```
Tambahkan catatan: respons aksi = `{ok, view, context}` (bukan `{state, log_delta}`); `view` = `session.view()`, `context` = `_context()` web.
- [ ] **Step 2:** Verifikasi: `rg -n "/api/game|/api/health|log_delta|game_id" docs/ENGINE_ARCHITECTURE.md` → tanpa match di §12.2; `rg -n "api/tianyuan|api/state|api/action|api/save" docs/ENGINE_ARCHITECTURE.md` → match.

---

### Task 4: ENGINE_ARCHITECTURE — §12.3 aksi aktual + klarifikasi Tianyuan Ling

**Files:** `docs/ENGINE_ARCHITECTURE.md` (baris 684-701)

- [ ] **Step 1:** Ganti baris `open_tianyuan` / `close_tianyuan` dengan catatan: panel Tianyuan Ling **bukan aksi mutasi sesi** — frontend memuat data via `GET /api/tianyuan` (modal) & status masuk `view()`; tidak ada handler `open_tianyuan`/`close_tianyuan` di `session.py`.
- [ ] **Step 2:** Tambah aksi yang ada di kode tapi belum terdokumentasi: `choose` (objektif pilih), `hunt` (berburu, gate respawn timer), `search` (cari herba material), `save` (`{"save_name"}` — titik aman, §13).
- [ ] **Step 3:** Verifikasi: handler map `session.py:112-128` (17 kunci) = union aksi di tabel + `dialog_choice`/`battle_action`/`choose`. `rg -n "open_tianyuan|close_tianyuan" docs/ENGINE_ARCHITECTURE.md` → hanya 1 kalimat klarifikasi (tanpa baris aksi).

---

### Task 5: ENGINE_ARCHITECTURE — §12.4 kontrak state sesuai view() aktual

**Files:** `docs/ENGINE_ARCHITECTURE.md` (baris 703-727)

- [ ] **Step 1:** Tulis ulang contoh `StateJSON` agar 100% cocok `session.view()` (session.py:485-563): kunci = `location` (id/name/description/is_safe/connections), `day`, `hour`, `player` (name/realm/realm_level/exp/exp_next/hp/hp_max/qi/qi_max/gold/roots/academy/morality/equipment), `current_quest`, `side_quests`, `inventory` (dengan `type`), `memories`, `companion` (atau `null`), `mode` (`explore|dialog|battle|choose`), `dialog`, `battle`, `choose`, `log`, `arc_summary` (ada saat `q_akademi_07` selesai).
- [ ] **Step 2:** Hapus kunci `quest`, `tianyuan`, `flags`, `time`, `ui` dari contoh; sesuaikan deskripsi mode & `choose` (objektif `kind: choose` → `choose.prompt/options`).
- [ ] **Step 3:** Verifikasi: `rg -n '"ui"|"quest"|"tianyuan"|"flags"|"time"' docs/ENGINE_ARCHITECTURE.md` → tanpa match di §12.4 (kunci lama hilang); setiap kunci contoh ada di `view()`.

---

### Task 6: ENGINE_ARCHITECTURE — §12.5 panel tianyuan modal + context

**Files:** `docs/ENGINE_ARCHITECTURE.md` (baris 741-746)

- [ ] **Step 1:** Detail implementasi: ganti "panel Tianyuan Ling toggle di kanan" → modal yang dibuka tombol "Baca Ingatan" / tombol panel, fetch `GET /api/tianyuan`, ditutup via tombol ✕.
- [ ] **Step 2:** Perjelas `context` = `_context()` web (npcs + can_spar/shop, merchant_shop, recipes, npc_names, techniques, academy, item_names).
- [ ] **Step 3:** Verifikasi: `rg -n "toggle|_context|npc_names|item_names|academy" docs/ENGINE_ARCHITECTURE.md` → match sesuai.

---

### Task 7: ENGINE_ARCHITECTURE — §15 tabel test + §17 fitur Verified

**Files:** `docs/ENGINE_ARCHITECTURE.md` (baris 785-832)

- [ ] **Step 1:** §15 tabel: tambah baris `test_effects.py` (dispatcher efek), `test_saveload.py` (round-trip save/load & save rusak ditolak). Perbarui deskripsi `test_web.py` jika perlu.
- [ ] **Step 2:** §17: tambah blok "**Fitur Fase 1 — SELESAI (Verified)**" berisi 6 fitur: Toko Web (modal beli/jual via `context.merchant_shop`), Dinamisasi Resep (`context.recipes` → tombol racik dinamis), Cooldown Side Quest (`side_quest_cooldowns`), Timer Respawn Monster (`last_hunt_time` vs `world.monster_respawn_hours`), Jadwal Harian NPC (`_is_npc_available`), Layar Penutup Arc 1 (`arc_summary` di `view()` + banner CLI + modal web). Pertahankan observasi playtest Fase 2 tetap terbuka.
- [ ] **Step 3:** Verifikasi: `rg -n "SELESAI \(Verified\)" docs/ENGINE_ARCHITECTURE.md` → match; observasi Han Xiu/over-leveling masih ada.

---

### Task 8: DESIGN_SUMMARY.md — header final + status penyelesaian Fase 1

**Files:** `docs/DESIGN_SUMMARY.md`

- [ ] **Step 1:** Header: ganti "untuk Review" → status final Fase 1 (Arc Akademi) **selesai & tervalidasi** + baris riwayat EP3-T2.
- [ ] **Step 2:** Tambah bagian **"8. Status Penyelesaian Fase 1"**: seluruh AC Fase 1 selesai; 6 fitur Verified (sama dgn Task 7 Step 2); rebalancing v0.1.0-alpha (exp quest −40%, grounding 2/jam, spar 8, hunt 6 → rajin ≈ Lv6); referensi ENGINE_ARCHITECTURE §16-17.
- [ ] **Step 3:** §6 tabel "Masih Bisa Diubah": tandai baris angka balancing → "sudah dikunci lewat rebalancing playtest". Baris lain (nama, akar spiritual, detail dialog) tetap bisa diganti di review konten.
- [ ] **Step 4:** Verifikasi: `rg -n "Status Penyelesaian|SELESAI|v0.1.0-alpha" docs/DESIGN_SUMMARY.md` → match.

---

### Task 9: Verifikasi AC lintas-doc + commit

- [ ] **Step 1:** `rg -n "/api/game|/api/health|game_id|log_delta|ui\.mode|open_tianyuan|close_tianyuan|belum diterapkan" docs/` → tidak ada match selain yang disengaja (kalimat klarifikasi).
- [ ] **Step 2:** Sanity kode (tidak diubah, hanya konfirmasi tidak rusak): `python3 -m pytest -q` (192 pass) — aman karena `data/`/`src/` tak disentuh; `python3 tools/validate_data.py` → exit 0.
- [ ] **Step 3:** `git status --short` → hanya 2 docs + 2 file superpowers (spec & plan ini).
- [ ] **Step 4:** Commit:
```bash
git add docs/ENGINE_ARCHITECTURE.md docs/DESIGN_SUMMARY.md docs/superpowers/specs/2026-08-14-ep3-t2-design.md docs/superpowers/plans/2026-08-14-ep3-t2.md
git commit -m "docs: sinkronisasi EP3-T2 ENGINE_ARCHITECTURE & DESIGN_SUMMARY ke kode nyata"
```
